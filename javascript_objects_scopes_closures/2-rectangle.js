#!/usr/bin/node
module.exports = class Rectangle {
    constructor(w, h) {
        this.width = w;
        this.height = h;
    }

};
if (this.height <= 0 || this.width <= 0) {
    const newObject = new Rectangle();
}