#!/usr/bin/python3
def complex_delete(a_dictionary, value):
    for i in a_dictionary:
        if a_dictionary[i] = value:
           return (del(a_dictionary[i]))
        else:
            return a_dictionary
        
